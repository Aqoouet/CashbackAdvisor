# Обзор проекта

## Что такое Open Cashback Advisor?

**Open Cashback Advisor** — это система для управления и оптимизации кэшбэков, состоящая из Telegram-бота и REST API сервера. Система позволяет пользователям создавать группы, добавлять кэшбэки и получать рекомендации по лучшим предложениям от различных банков.

## Основные возможности

### 🤖 Telegram Бот
- Интуитивный интерфейс для работы с кэшбэками
- Поддержка групп пользователей для совместного использования кэшбэков
- Автоматическая валидация и предложения при добавлении кэшбэков
- Поиск лучших кэшбэков по категориям
- Управление кэшбэками (создание, просмотр, обновление, удаление)

### 🌐 REST API
- Полнофункциональный REST API для интеграции с внешними системами
- Поддержка fuzzy-поиска для исправления опечаток
- Валидация данных на уровне API
- Пагинация для больших списков
- CORS поддержка для веб-приложений

### 🗄️ База данных
- PostgreSQL с расширением `pg_trgm` для нечеткого поиска
- Индексы для оптимизации запросов
- Автоматическое обновление временных меток
- Миграции для управления схемой БД

## Технологический стек

- **Язык программирования**: Go 1.22
- **База данных**: PostgreSQL 15
- **Telegram Bot API**: go-telegram-bot-api/v5
- **HTTP Router**: Chi v5
- **Контейнеризация**: Docker и Docker Compose
- **Миграции БД**: SQL скрипты

## Архитектура системы

Система состоит из трех основных компонентов:

1. **Telegram Bot** (`cmd/bot`) — обрабатывает сообщения пользователей и взаимодействует с API
2. **HTTP API Server** (`cmd/server`) — предоставляет REST API для управления кэшбэками
3. **PostgreSQL Database** — хранит все данные о кэшбэках и группах

Все компоненты работают в Docker-контейнерах и общаются через внутреннюю сеть.

## Ключевые концепции

### Группы пользователей
Пользователи объединяются в группы для совместного использования кэшбэков. Каждый пользователь может состоять только в одной группе. Группы позволяют:
- Совместно управлять кэшбэками
- Получать рекомендации на основе кэшбэков всех участников группы
- Организовывать команды или семьи для оптимизации кэшбэков

### Кэшбэки
Каждый кэшбэк (запись) содержит:
- **Группа** — к какой группе относится кэшбэк
- **Категория** — категория покупок (например, "Рестораны", "Транспорт", "Все покупки")
- **Банк** — название банка
- **Пользователь** — кто добавил кэшбэк
- **Месяц/Год** — период действия кэшбэка
- **Процент кэшбэка** — процент возврата (0-100%)
- **Максимальная сумма** — максимальная сумма кэшбэка

### Fuzzy-поиск
Система использует триграммный поиск PostgreSQL для исправления опечаток в названиях групп, категорий, банков и имен пользователей. Это позволяет находить похожие значения даже при наличии ошибок.

## Сценарии использования

1. **Семейное управление кэшбэками**: Семья создает группу и добавляет кэшбэки от разных банков, чтобы всегда знать, какой картой лучше платить
2. **Командная работа**: Команда коллег делится информацией о выгодных предложениях банков
3. **Персональное использование**: Один пользователь может использовать систему для отслеживания своих кэшбэков

## Преимущества системы

- ✅ **Простота использования**: Telegram-бот с интуитивным интерфейсом
- ✅ **Гибкость**: REST API для интеграции с другими системами
- ✅ **Надежность**: Валидация данных и обработка ошибок
- ✅ **Масштабируемость**: Docker-контейнеризация для легкого развертывания
- ✅ **Производительность**: Оптимизированные индексы БД и fuzzy-поиск

## Структура проекта

```
Bot/
├── cmd/
│   ├── bot/          # Telegram бот
│   └── server/       # HTTP API сервер
├── internal/
│   ├── bot/          # Логика Telegram бота
│   ├── config/       # Конфигурация
│   ├── database/     # Работа с БД
│   ├── handlers/     # HTTP обработчики
│   ├── models/       # Модели данных
│   ├── service/      # Бизнес-логика
│   └── validator/   # Валидация данных
├── migrations/       # SQL миграции
├── docs/             # Документация
└── docker-compose.yml
```

## Следующие шаги

- [Установка и настройка](02-installation.md) — как установить и запустить систему
- [Архитектура](02-architecture.md) — детальное описание архитектуры
- [API Справочник](04-api-reference.md) — документация REST API
- [Команды бота](05-bot-commands.md) — список команд Telegram бота

