# Вклад в проект

Спасибо за интерес к проекту! Мы приветствуем вклад от всех желающих.

## Как внести вклад

### Типы вклада

Мы принимаем следующие виды вклада:

- 🐛 **Исправление багов** — найдите и исправьте ошибки
- ✨ **Новые функции** — добавьте новые возможности
- 📚 **Документация** — улучшите документацию
- 🧪 **Тесты** — добавьте или улучшите тесты
- 🎨 **Улучшения UI/UX** — улучшите интерфейс бота
- ⚡ **Оптимизация** — улучшите производительность
- 🔧 **Рефакторинг** — улучшите качество кода

---

## Процесс разработки

### 1. Форк и клонирование

```bash
# Создайте форк репозитория на GitHub
# Затем клонируйте свой форк
git clone https://github.com/your-username/Bot.git
cd Bot

# Добавьте оригинальный репозиторий как upstream
git remote add upstream https://github.com/original-owner/Bot.git
```

### 2. Создание ветки

```bash
# Создайте ветку для вашей работы
git checkout -b feature/your-feature-name

# Или для исправления бага
git checkout -b fix/bug-description
```

**Соглашения о названиях веток**:
- `feature/` — новые функции
- `fix/` — исправления багов
- `docs/` — изменения в документации
- `refactor/` — рефакторинг
- `test/` — добавление тестов

### 3. Разработка

- Следуйте стандартам кода (см. ниже)
- Пишите понятный и читаемый код
- Добавляйте комментарии там, где это необходимо
- Пишите тесты для новых функций

### 4. Коммиты

**Соглашения о коммитах**:

Используйте префиксы для коммитов:
- `feat:` — новая функция
- `fix:` — исправление бага
- `docs:` — изменения в документации
- `style:` — форматирование кода (не влияет на логику)
- `refactor:` — рефакторинг
- `test:` — добавление тестов
- `chore:` — обновление зависимостей, конфигурации и т.д.

**Примеры**:
```
feat: добавлена команда /bankinfo для просмотра кэшбэков банка
fix: исправлена ошибка валидации даты в формате DD.MM.YYYY
docs: обновлена документация API
refactor: оптимизирован fuzzy-поиск по категориям
test: добавлены тесты для валидатора
```

**Формат коммита**:
```
<тип>: <краткое описание>

<подробное описание (опционально)>

<ссылки на issues (опционально)>
```

### 5. Push и Pull Request

```bash
# Отправьте изменения в свой форк
git push origin feature/your-feature-name

# Создайте Pull Request на GitHub
```

**Перед созданием PR**:
- ✅ Код компилируется без ошибок
- ✅ Все тесты проходят
- ✅ Код отформатирован (`make fmt`)
- ✅ Линтер не выдает ошибок (`make lint`)
- ✅ Документация обновлена (если нужно)
- ✅ Коммиты следуют соглашениям

---

## Стандарты кода

### Go Style Guide

Следуйте официальному [Go Code Review Comments](https://github.com/golang/go/wiki/CodeReviewComments).

**Основные правила**:

1. **Именование**:
   - Экспортируемые функции/типы: `PascalCase`
   - Неэкспортируемые: `camelCase`
   - Константы: `UPPER_CASE` или `PascalCase`

2. **Форматирование**:
   ```bash
   make fmt
   ```

3. **Комментарии**:
   - Экспортируемые функции должны иметь комментарии
   - Используйте полные предложения

4. **Обработка ошибок**:
   - Всегда обрабатывайте ошибки
   - Используйте `fmt.Errorf` с `%w` для обертки ошибок

### Структура кода

**Пакеты**:
- Один пакет = одна ответственность
- Избегайте циклических зависимостей

**Функции**:
- Делайте функции короткими и понятными
- Одна функция = одна задача
- Максимальная длина функции: ~50 строк

**Тесты**:
- Тесты в файлах `*_test.go`
- Используйте табличные тесты для множественных случаев
- Покрытие тестами: минимум 70%

---

## Тестирование

### Запуск тестов

```bash
# Все тесты
make test

# Конкретный пакет
go test ./internal/validator/...

# С покрытием
go test -cover ./...
```

### Написание тестов

**Пример табличного теста**:
```go
func TestValidateTextField(t *testing.T) {
    tests := []struct {
        name    string
        field   string
        wantErr bool
    }{
        {
            name:    "valid field",
            field:   "Test",
            wantErr: false,
        },
        {
            name:    "empty field",
            field:   "",
            wantErr: true,
        },
    }
    
    for _, tt := range tests {
        t.Run(tt.name, func(t *testing.T) {
            err := ValidateTextField("test", tt.field, true)
            if (err != nil) != tt.wantErr {
                t.Errorf("ValidateTextField() error = %v, wantErr %v", err, tt.wantErr)
            }
        })
    }
}
```

---

## Документация

### Обновление документации

При добавлении новых функций обновите соответствующую документацию:

- **API**: `docs/04-api-reference.md`
- **Команды бота**: `docs/05-bot-commands.md`
- **База данных**: `docs/06-database.md` (если изменена схема)
- **Разработка**: `docs/08-development.md` (если добавлены новые инструменты)

### Комментарии в коде

```go
// ValidateTextField проверяет текстовое поле на корректность.
// Параметры:
//   - name: название поля для сообщений об ошибках
//   - value: значение для проверки
//   - required: обязательность поля
// Возвращает ошибку, если поле невалидно.
func ValidateTextField(name, value string, required bool) error {
    // ...
}
```

---

## Pull Request процесс

### Шаблон PR

При создании Pull Request укажите:

1. **Описание изменений**:
   - Что изменено
   - Зачем это нужно
   - Как это работает

2. **Тип изменений**:
   - [ ] Исправление бага
   - [ ] Новая функция
   - [ ] Изменения в документации
   - [ ] Рефакторинг
   - [ ] Тесты

3. **Проверка**:
   - [ ] Код компилируется
   - [ ] Тесты проходят
   - [ ] Документация обновлена
   - [ ] Линтер не выдает ошибок

4. **Скриншоты** (если применимо)

### Code Review

После создания PR:
- Код будет проверен мейнтейнерами
- Могут быть запрошены изменения
- После одобрения изменения будут влиты в основную ветку

**Что проверяется**:
- Соответствие стандартам кода
- Корректность логики
- Наличие тестов
- Обновление документации
- Производительность (если применимо)

---

## Сообщение о багах

### Создание Issue

При создании Issue укажите:

1. **Описание проблемы**:
   - Что произошло
   - Что ожидалось

2. **Шаги для воспроизведения**:
   ```
   1. Сделайте это
   2. Затем это
   3. Ошибка происходит здесь
   ```

3. **Окружение**:
   - ОС и версия
   - Версия Go
   - Версия Docker (если используется)
   - Версия приложения

4. **Логи** (если применимо):
   ```
   [вставьте логи здесь]
   ```

5. **Скриншоты** (если применимо)

---

## Предложение новых функций

### Создание Feature Request

При предложении новой функции укажите:

1. **Описание функции**:
   - Что вы хотите добавить
   - Зачем это нужно
   - Как это будет работать

2. **Примеры использования**:
   - Как пользователь будет использовать функцию
   - Примеры команд/запросов

3. **Альтернативы**:
   - Рассматривали ли вы альтернативные решения?

4. **Дополнительная информация**:
   - Связанные issues
   - Референсы на похожие функции в других проектах

---

## Вопросы и обсуждения

### Где задавать вопросы

- **Общие вопросы**: создайте Discussion на GitHub
- **Быстрые вопросы**: используйте Issues
- **Безопасность**: отправьте email мейнтейнерам (не создавайте публичный Issue)

---

## Лицензия

Внося вклад в проект, вы соглашаетесь с тем, что ваш вклад будет лицензирован под той же лицензией, что и проект.

---

## Благодарности

Спасибо всем, кто вносит вклад в проект! Ваша помощь делает проект лучше.

---

## Полезные ссылки

- [Go Code Review Comments](https://github.com/golang/go/wiki/CodeReviewComments)
- [Effective Go](https://go.dev/doc/effective_go)
- [Go Testing Best Practices](https://golang.org/doc/effective_go#testing)
- [Conventional Commits](https://www.conventionalcommits.org/)

---

## Следующие шаги

- [Разработка](08-development.md) — настройка окружения разработки
- [Архитектура](02-architecture.md) — понимание архитектуры проекта
- [Решение проблем](09-troubleshooting.md) — помощь в решении проблем

