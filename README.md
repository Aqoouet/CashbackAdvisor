# 🏦 Open Cashback Advisor

Микросервис на Go для совместного ведения базы правил кэшбэка с fuzzy-поиском и строгой валидацией.

## 🎯 Особенности

- ✅ **Строгая валидация** всех числовых и временных полей
- 🔍 **Fuzzy-поиск** с использованием PostgreSQL pg_trgm для исправления опечаток
- 🚀 **REST API** с полным набором CRUD операций
- 🔒 **Транзакционность** и безопасность при конкурентном доступе
- 📊 **Умные предложения** для исправления ошибок ввода
- 🐳 **Docker** для быстрого развертывания

## 🛠 Технологический стек

- **Язык:** Go 1.22+
- **База данных:** PostgreSQL 15+ с расширением pg_trgm
- **Роутер:** Chi v5
- **Драйвер БД:** pgx/v5
- **Контейнеризация:** Docker & Docker Compose

## 📋 Требования

- Go 1.22 или выше
- PostgreSQL 15 или выше
- Docker (опционально)
- Make (опционально)

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone https://github.com/rymax1e/open-cashback-advisor.git
cd open-cashback-advisor
```

### 2. Настройка окружения

Создайте файл `.env` на основе `.env.example`:

```bash
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=cashback_db
DB_SSLMODE=disable

SERVER_HOST=0.0.0.0
SERVER_PORT=8080
```

### 3. Запуск с Docker (рекомендуется)

```bash
# Запуск PostgreSQL
make docker-up

# Применение миграций
make migrate

# Запуск сервера
make run
```

### 4. Запуск без Docker

```bash
# Установка зависимостей
make deps

# Применение миграций (PostgreSQL должен быть запущен)
make migrate

# Запуск сервера
make run
```

Сервер будет доступен по адресу: `http://localhost:8080`

## 📚 API Документация

### Базовый URL

```
http://localhost:8080/api/v1
```

### Эндпоинты

#### 1. Анализ и предложения (`POST /cashback/suggest`)

Анализирует введенные данные и возвращает предложения по исправлению.

**Запрос:**
```json
{
  "group_name": "Транспорт",
  "category": "такси",
  "bank_name": "Тинькоф",
  "user_display_name": "Иван",
  "month_year": "2024-12",
  "cashback_percent": 5.5,
  "max_amount": 3000.0
}
```

**Ответ (200 OK):**
```json
{
  "valid": true,
  "suggestions": {
    "bank_name": [
      {
        "value": "Тинькофф",
        "similarity": 0.85
      }
    ],
    "category": [
      {
        "value": "Такси",
        "similarity": 0.75
      }
    ]
  },
  "can_proceed": true
}
```

#### 2. Создание правила (`POST /cashback`)

Создает новое правило кэшбэка.

**Запрос:**
```json
{
  "group_name": "Транспорт",
  "category": "Такси",
  "bank_name": "Тинькофф",
  "user_id": "123456789",
  "user_display_name": "Иван",
  "month_year": "2024-12",
  "cashback_percent": 5.5,
  "max_amount": 3000.0,
  "force": false
}
```

**Ответ (201 Created):**
```json
{
  "id": 1,
  "group_name": "Транспорт",
  "category": "Такси",
  "bank_name": "Тинькофф",
  "user_id": "123456789",
  "user_display_name": "Иван",
  "month_year": "2024-12-01T00:00:00Z",
  "cashback_percent": 5.5,
  "max_amount": 3000.0,
  "created_at": "2024-12-09T10:00:00Z",
  "updated_at": "2024-12-09T10:00:00Z"
}
```

#### 3. Получение лучшего кэшбэка (`GET /cashback/best`)

Возвращает правило с максимальным процентом кэшбэка.

**Запрос:**
```
GET /api/v1/cashback/best?group_name=Транспорт&category=Такси&month_year=2024-12
```

**Ответ (200 OK):**
```json
{
  "id": 1,
  "group_name": "Транспорт",
  "category": "Такси",
  "bank_name": "Тинькофф",
  "user_id": "123456789",
  "user_display_name": "Иван",
  "month_year": "2024-12-01T00:00:00Z",
  "cashback_percent": 5.5,
  "max_amount": 3000.0,
  "created_at": "2024-12-09T10:00:00Z",
  "updated_at": "2024-12-09T10:00:00Z"
}
```

#### 4. Список правил (`GET /cashback`)

Получает список всех правил с пагинацией.

**Запрос:**
```
GET /api/v1/cashback?limit=20&offset=0&user_id=123456789
```

**Ответ (200 OK):**
```json
{
  "rules": [...],
  "total": 42,
  "limit": 20,
  "offset": 0
}
```

#### 5. Получение правила (`GET /cashback/{id}`)

Получает правило по ID.

**Запрос:**
```
GET /api/v1/cashback/1
```

#### 6. Обновление правила (`PUT /cashback/{id}`)

Обновляет существующее правило.

**Запрос:**
```json
{
  "cashback_percent": 6.0,
  "max_amount": 3500.0
}
```

#### 7. Удаление правила (`DELETE /cashback/{id}`)

Удаляет правило по ID.

**Запрос:**
```
DELETE /api/v1/cashback/1
```

#### 8. Проверка здоровья (`GET /health`)

Проверяет состояние сервиса.

**Ответ (200 OK):**
```json
{
  "status": "ok"
}
```

## 🔍 Валидация

### Правила валидации

| Поле | Формат | Ограничения |
|------|--------|-------------|
| `month_year` | `YYYY-MM` | Корректная дата (например, `2024-12`) |
| `cashback_percent` | Число | 0.00 ≤ x ≤ 100.00, округление до 2 знаков |
| `max_amount` | Число | ≥ 0.00, округление до 2 знаков |
| Текстовые поля | Строка | Макс. 500 символов, не пустые |

### Пороги схожести для fuzzy-поиска

| Поле | Порог |
|------|-------|
| `group_name` | 0.6 |
| `category` | 0.6 |
| `bank_name` | 0.65 |
| `user_display_name` | 0.7 |

## 🧪 Тестирование

```bash
# Запуск всех тестов
make test

# Запуск тестов с покрытием
go test -v -race -coverprofile=coverage.out ./...
go tool cover -html=coverage.out
```

## 📦 Сборка

```bash
# Сборка бинарного файла
make build

# Запуск бинарного файла
./bin/server
```

## 🔧 Утилиты Make

```bash
make help          # Показать все доступные команды
make build         # Собрать приложение
make run           # Запустить сервер
make test          # Запустить тесты
make clean         # Очистить сгенерированные файлы
make migrate       # Применить миграции
make rollback      # Откатить миграции
make deps          # Установить зависимости
make docker-up     # Запустить PostgreSQL в Docker
make docker-down   # Остановить Docker контейнеры
make dev           # Запустить среду разработки (docker + migrate + run)
make fmt           # Форматирование кода
make lint          # Проверка линтером
```

## 📁 Структура проекта

```
.
├── cmd/
│   └── server/
│       └── main.go              # Точка входа
├── internal/
│   ├── config/
│   │   └── config.go            # Конфигурация
│   ├── database/
│   │   ├── database.go          # Подключение к БД
│   │   └── repository.go        # Репозиторий
│   ├── handlers/
│   │   └── handlers.go          # HTTP обработчики
│   ├── models/
│   │   └── cashback.go          # Модели данных
│   ├── service/
│   │   └── service.go           # Бизнес-логика
│   └── validator/
│       └── validator.go         # Валидация
├── migrations/
│   ├── 001_initial_schema.sql   # Начальная схема
│   └── 002_down.sql             # Откат
├── scripts/
│   ├── migrate.sh               # Скрипт миграций
│   └── rollback.sh              # Скрипт отката
├── docker-compose.yml           # Docker конфигурация
├── Makefile                     # Утилиты сборки
├── go.mod                       # Go модули
└── README.md                    # Документация
```

## 🤝 Интеграция с Telegram ботом

Микросервис легко интегрируется с Telegram ботом:

1. Бот получает данные от пользователя
2. Вызывает `/suggest` для проверки и получения предложений
3. Показывает пользователю предложения по исправлению
4. После подтверждения вызывает `/cashback` для сохранения

## 📝 Примеры запросов

Смотрите файл `examples/requests.http` для примеров запросов в формате REST Client.

## 🐛 Отладка

```bash
# Просмотр логов Docker
docker-compose logs -f postgres

# Подключение к PostgreSQL
psql -h localhost -U postgres -d cashback_db

# Проверка таблиц
\dt

# Проверка индексов
\di
```

## 📄 Лицензия

MIT License

## 👤 Автор

rymax1e - [GitHub](https://github.com/rymax1e)

---

**Примечание:** Это демонстрационный проект для портфолио, показывающий навыки работы с Go, PostgreSQL, REST API и современными практиками разработки.

