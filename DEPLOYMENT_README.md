# 🚀 Деплой CashbackAdvisor - Полное руководство

## 📁 Файлы для деплоя

После настройки деплоя в проекте появились следующие файлы:

### Основные файлы конфигурации:

| Файл | Описание |
|------|----------|
| `env.example` | Шаблон для создания `.env` файла с переменными окружения |
| `.env` | **Локальный файл** с токенами и паролями (НЕ коммитится в git) |
| `.gitignore` | Список файлов, игнорируемых git (включая `.env`) |

### Скрипты деплоя:

| Файл | Описание | Использование |
|------|----------|---------------|
| `deploy.sh` | Скрипт для деплоя **на сервере** | `./deploy.sh` |
| `remote-deploy.sh` | Скрипт для деплоя **с локальной машины** | `./remote-deploy.sh` |

### Docker файлы:

| Файл | Описание |
|------|----------|
| `Dockerfile` | Образ для API сервера |
| `Dockerfile.bot` | Образ для Telegram бота |
| `docker-compose.yml` | Конфигурация только для PostgreSQL (разработка) |
| `docker-compose.full.yml` | Полный стек: PostgreSQL + API + Bot (продакшн) |

### Документация:

| Файл | Описание |
|------|----------|
| `QUICKDEPLOY.md` | 🎯 **Быстрый старт** - читайте в первую очередь |
| `DEPLOY.md` | Полная инструкция по деплою |
| `SSH_SETUP.md` | Настройка SSH ключей для безопасного подключения |
| `DEPLOYMENT_README.md` | Этот файл - общий обзор деплоя |

---

## 🎯 Быстрый старт (за 5 минут)

### 1. Получите токен бота

Откройте Telegram → [@BotFather](https://t.me/botfather) → `/newbot`

### 2. Установите токен

```bash
export TELEGRAM_BOT_TOKEN="ваш_токен_от_BotFather"
```

### 3. Отправьте код в GitHub

```bash
git add .
git commit -m "Ready for deployment"
git push origin main
```

### 4. Разверните на сервере

**Вариант A: Автоматический деплой с локальной машины**

```bash
chmod +x remote-deploy.sh
./remote-deploy.sh
```

**Вариант B: Ручной деплой на сервере**

```bash
# Подключитесь к серверу
ssh cashback@82.26.150.98

# Клонируйте репозиторий
git clone https://github.com/Aqoouet/CashbackAdvisor.git
cd CashbackAdvisor

# Установите токен и запустите деплой
export TELEGRAM_BOT_TOKEN="ваш_токен"
chmod +x deploy.sh
./deploy.sh
```

### 5. Проверьте работу

```bash
# На сервере
docker-compose -f docker-compose.full.yml ps
docker-compose -f docker-compose.full.yml logs -f bot
```

🎉 **Готово!** Бот работает!

---

## 📊 Архитектура деплоя

```
┌─────────────────────────────────────────────────────┐
│                   Ваш Сервер                        │
│              82.26.150.98                           │
│                                                     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────┐ │
│  │  PostgreSQL  │  │   API Server │  │    Bot   │ │
│  │   (порт      │◄─┤   (порт      │◄─┤          │ │
│  │    5432)     │  │    8080)     │  │          │ │
│  └──────────────┘  └──────────────┘  └──────────┘ │
│         ▲                  ▲                ▲       │
│         │                  │                │       │
│         └──────────────────┴────────────────┘       │
│              Docker Network (cashback-network)      │
└─────────────────────────────────────────────────────┘
                         ▲
                         │
                    Telegram API
```

### Компоненты:

1. **PostgreSQL** - База данных для хранения правил кэшбэка
2. **API Server** - REST API на Go для работы с данными
3. **Telegram Bot** - Бот для взаимодействия с пользователями

Все компоненты запускаются в Docker контейнерах и общаются через внутреннюю сеть.

---

## 🔐 Безопасность: Где хранить токен?

### ✅ ПРАВИЛЬНО:

#### На локальной машине:
```bash
# В файле ~/.bashrc или ~/.zshrc
echo 'export TELEGRAM_BOT_TOKEN="ваш_токен"' >> ~/.bashrc
source ~/.bashrc
```

#### На сервере:
```bash
# В файле .env в директории проекта
cd CashbackAdvisor
nano .env
```

Содержимое `.env`:
```env
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
DB_PASSWORD=надёжный_пароль_БД
```

### ❌ НЕ ДЕЛАЙТЕ ТАК:

- ❌ **НЕ** добавляйте `.env` в git
- ❌ **НЕ** храните токен в коде
- ❌ **НЕ** передавайте токен через незащищённые каналы
- ❌ **НЕ** публикуйте токен в публичных репозиториях

---

## 🔧 Управление после деплоя

### Просмотр логов

```bash
# Все сервисы
docker-compose -f docker-compose.full.yml logs -f

# Только бот
docker-compose -f docker-compose.full.yml logs -f bot

# Только API
docker-compose -f docker-compose.full.yml logs -f api

# База данных
docker-compose -f docker-compose.full.yml logs -f postgres
```

### Перезапуск сервисов

```bash
# Перезапуск бота
docker-compose -f docker-compose.full.yml restart bot

# Перезапуск API
docker-compose -f docker-compose.full.yml restart api

# Перезапуск всего
docker-compose -f docker-compose.full.yml restart
```

### Остановка и запуск

```bash
# Остановка
docker-compose -f docker-compose.full.yml stop

# Запуск
docker-compose -f docker-compose.full.yml start

# Остановка и удаление контейнеров
docker-compose -f docker-compose.full.yml down
```

### Обновление кода

```bash
cd CashbackAdvisor
git pull origin main
docker-compose -f docker-compose.full.yml down
docker-compose -f docker-compose.full.yml build
docker-compose -f docker-compose.full.yml up -d
```

---

## 🆘 Troubleshooting

### Бот не отвечает

1. **Проверьте логи:**
   ```bash
   docker-compose -f docker-compose.full.yml logs bot
   ```

2. **Проверьте токен:**
   ```bash
   cat .env | grep TELEGRAM_BOT_TOKEN
   ```

3. **Перезапустите бота:**
   ```bash
   docker-compose -f docker-compose.full.yml restart bot
   ```

### API не работает

1. **Проверьте логи API:**
   ```bash
   docker-compose -f docker-compose.full.yml logs api
   ```

2. **Проверьте подключение к БД:**
   ```bash
   docker-compose -f docker-compose.full.yml logs postgres
   ```

3. **Проверьте health endpoint:**
   ```bash
   curl http://localhost:8080/health
   ```

### База данных не запускается

1. **Проверьте логи PostgreSQL:**
   ```bash
   docker-compose -f docker-compose.full.yml logs postgres
   ```

2. **Проверьте, не занят ли порт:**
   ```bash
   sudo netstat -tulpn | grep 5432
   ```

3. **Удалите volumes и создайте заново:**
   ```bash
   docker-compose -f docker-compose.full.yml down -v
   docker-compose -f docker-compose.full.yml up -d
   ```

### Контейнеры постоянно перезапускаются

```bash
# Посмотрите статус
docker-compose -f docker-compose.full.yml ps

# Проверьте логи проблемного контейнера
docker-compose -f docker-compose.full.yml logs [имя_контейнера]
```

---

## 📦 Резервное копирование

### Резервная копия базы данных

```bash
# Создание бэкапа
docker exec cashback_postgres pg_dump -U postgres cashback_db > backup_$(date +%Y%m%d_%H%M%S).sql

# Восстановление из бэкапа
cat backup_20241209_120000.sql | docker exec -i cashback_postgres psql -U postgres -d cashback_db
```

### Резервная копия .env файла

```bash
# Создайте зашифрованную копию
tar -czf env-backup.tar.gz .env
gpg -c env-backup.tar.gz
rm env-backup.tar.gz

# Храните env-backup.tar.gz.gpg в безопасном месте!
```

---

## 🔄 Автоматические обновления

### Настройка автоматического обновления кода

Создайте скрипт `auto-update.sh`:

```bash
#!/bin/bash
cd /home/cashback/CashbackAdvisor
git pull origin main
docker-compose -f docker-compose.full.yml build
docker-compose -f docker-compose.full.yml up -d
```

Добавьте в crontab (для обновления каждый день в 3:00):

```bash
crontab -e
# Добавьте строку:
0 3 * * * /home/cashback/CashbackAdvisor/auto-update.sh >> /home/cashback/update.log 2>&1
```

---

## 📊 Мониторинг

### Использование ресурсов

```bash
# Использование ресурсов контейнерами
docker stats

# Размер контейнеров
docker system df

# Логи Docker
journalctl -u docker.service
```

### Проверка здоровья

```bash
# API health check
curl http://localhost:8080/health

# Проверка всех контейнеров
docker-compose -f docker-compose.full.yml ps
```

---

## 📚 Дополнительные ресурсы

- **[QUICKDEPLOY.md](QUICKDEPLOY.md)** - Быстрая инструкция для начала
- **[DEPLOY.md](DEPLOY.md)** - Детальное руководство по деплою
- **[SSH_SETUP.md](SSH_SETUP.md)** - Настройка SSH ключей
- **[ARCHITECTURE.md](ARCHITECTURE.md)** - Архитектура приложения
- **[README.md](README.md)** - Основная документация проекта

---

## 💡 Полезные команды

### Docker

```bash
# Остановить все контейнеры
docker stop $(docker ps -aq)

# Удалить все контейнеры
docker rm $(docker ps -aq)

# Очистка неиспользуемых образов
docker system prune -a

# Просмотр логов контейнера
docker logs -f [container_id]
```

### Git

```bash
# Проверка статуса
git status

# Получение последних изменений
git pull origin main

# Отправка изменений
git add .
git commit -m "Update"
git push origin main
```

### Системные

```bash
# Проверка использования диска
df -h

# Проверка использования памяти
free -h

# Процессы
htop  # или top
```

---

## ✅ Чек-лист перед продакшном

- [ ] Токен бота получен и сохранён в безопасном месте
- [ ] Файл `.env` создан и не добавлен в git
- [ ] `.gitignore` настроен правильно
- [ ] SSH ключи настроены для безопасного доступа
- [ ] Firewall настроен (порты 22, 8080)
- [ ] Установлены автоматические обновления системы
- [ ] Настроено резервное копирование базы данных
- [ ] Проверена работа всех эндпоинтов API
- [ ] Бот протестирован в Telegram
- [ ] Логи мониторятся
- [ ] Контейнеры настроены на автоперезапуск

---

## 🤝 Поддержка

Если возникли вопросы:

1. Проверьте логи: `docker-compose -f docker-compose.full.yml logs -f`
2. Прочитайте [DEPLOY.md](DEPLOY.md) и [QUICKDEPLOY.md](QUICKDEPLOY.md)
3. Проверьте issues в GitHub репозитории

---

**Успешного деплоя! 🚀**

